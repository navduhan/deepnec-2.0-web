'use client';

import { useEffect, useMemo, useState } from 'react';
import Link from 'next/link';
import dynamic from 'next/dynamic';
import { CheckCircle2, Download, ExternalLink, FlaskConical, Loader2 } from 'lucide-react';
import { withBasePath } from '@/lib/base-path';
import { expiresAt, readJobBookmark, type JobBookmark } from '@/lib/job-bookmark';

const StructureViewer = dynamic(() => import('@/components/StructureViewer'), { ssr: false });
type Value=string|number|boolean|null; type Row=Record<string,Value>;
type Result={jobId?:string;status?:string;error?:string;updatedAt?:string;results?:Record<string,Row[]>};
type Secondary={sampleId:string;amino_acids:string;prediction:string;confidence:string};
type Tertiary={sampleId:string;pdb:string;sequence:string;method:string};
const rank=(n:string)=>n.includes('Phase_1')?1:n.includes('Phase_2')?2:n.includes('Phase_3')?3:n.includes('Phase_4')?4:n.includes('motif')?5:9;
const meta=(n:string)=>n.includes('Phase_1')?['Phase 1','Enzyme filter']:n.includes('Phase_2')?['Phase 2','Nitrogen filter']:n.includes('Phase_3')?['Phase 3','Pathway classification']:n.includes('Phase_4')?['Phase 4','EC assignment']:n.includes('motif')?['Motifs','Sequence motif scan']:['Output',n];
const idOf=(row:Row)=>String(row.SampleID??row.Sequence_ID??'');
const tsv=(rows:Row[])=>rows.length?[Object.keys(rows[0]).join('\t'),...rows.map(r=>Object.keys(rows[0]).map(k=>String(r[k]??'')).join('\t'))].join('\n'):'';
const csv=(rows:Row[])=>rows.length?[Object.keys(rows[0]),...rows.map(r=>Object.keys(rows[0]).map(k=>r[k]??''))].map(row=>row.map(value=>`"${String(value).replaceAll('"','""')}"`).join(',')).join('\n'):'';
function save(content:string,name:string,type='text/tab-separated-values'){const url=URL.createObjectURL(new Blob([content],{type}));const a=document.createElement('a');a.href=url;a.download=name;a.click();URL.revokeObjectURL(url);}
async function fetchJob(b:JobBookmark){const response=await fetch(withBasePath(`/api/predict?jobId=${encodeURIComponent(b.jobId)}`),{cache:'no-store',headers:{'X-DeepNEC-Job-Token':b.jobToken}});const data=await response.json() as Result;if(!response.ok)throw new Error(data.error||'Result was not found.');return data;}
const links=(ec:string)=>[['NCBI',`https://www.ncbi.nlm.nih.gov/protein/?term=${encodeURIComponent(ec)}`],['UniProt',`https://www.uniprot.org/uniprotkb?query=${encodeURIComponent(ec)}`],['BRENDA',`https://www.brenda-enzymes.org/enzyme.php?ecno=${encodeURIComponent(ec)}`],['KEGG',`https://www.genome.jp/dbget-bin/www_bget?ec:${encodeURIComponent(ec)}`],['JGI',`https://img.jgi.doe.gov/cgi-bin/m/main.cgi?section=FindFunctions&page=EnzymeGenomeList&gtype=isolate&ec_number=EC:${encodeURIComponent(ec)}`]];

export default function ResultsPage(){
  const [data,setData]=useState<Result|null>(null),[loaded,setLoaded]=useState(false),[error,setError]=useState(''),[active,setActive]=useState(''),[busy,setBusy]=useState('');
  const [bookmark,setBookmark]=useState<JobBookmark|null>(null),[secondary,setSecondary]=useState<Secondary|null>(null),[tertiary,setTertiary]=useState<Tertiary|null>(null);
  useEffect(()=>{let cancelled=false;void Promise.resolve().then(async()=>{const b=readJobBookmark();if(!cancelled)setBookmark(b);try{let d=b?await fetchJob(b):JSON.parse(localStorage.getItem('deepnec_last_results')||'null') as Result|null;if(b&&d)while(!cancelled&&(d.status==='queued'||d.status==='running')){await new Promise(r=>setTimeout(r,3000));d=await fetchJob(b);}if(d?.status==='failed')throw new Error(d.error||'Prediction failed.');if(!cancelled&&d){setData(d);setActive(Object.keys(d.results||{}).sort((a,c)=>rank(a)-rank(c))[0]||'');localStorage.setItem('deepnec_last_results',JSON.stringify(d));}}catch(e:unknown){if(!cancelled)setError(e instanceof Error?e.message:'Unable to load result.');}finally{if(!cancelled)setLoaded(true);}});return()=>{cancelled=true};},[]);
  const files=useMemo(()=>Object.keys(data?.results||{}).sort((a,b)=>rank(a)-rank(b)),[data]);const rows=data?.results?.[active]||[];const expiry=data?.updatedAt?expiresAt(data.updatedAt):null;
  async function predict(kind:'secondary'|'tertiary',sampleId:string){if(!bookmark)return;setBusy(`${kind}:${sampleId}`);setError('');try{const response=await fetch(withBasePath(`/api/structure/${kind}`),{method:'POST',headers:{'Content-Type':'application/json','X-DeepNEC-Job-Token':bookmark.jobToken},body:JSON.stringify({jobId:bookmark.jobId,sampleId})});const value=await response.json();if(!response.ok)throw new Error(value.error||'Structure prediction failed.');if(kind==='secondary')setSecondary(value);else setTertiary(value);}catch(e:unknown){setError(e instanceof Error?e.message:'Structure prediction failed.');}finally{setBusy('');}}
  if(!loaded)return <div className="py-20 text-center"><Loader2 className="mx-auto h-7 w-7 animate-spin text-[var(--blue)]"/></div>;
  if(!data||!files.length)return <div className="mx-auto max-w-3xl py-16"><section className="surface p-10 text-center"><FlaskConical className="mx-auto h-8 w-8 text-[var(--pink)]"/><h1 className="mt-5 font-display text-4xl font-semibold">No results available</h1><p className="mt-3 text-sm text-slate-600">{error||'Submit protein FASTA to create results.'}</p><Link href="/prediction" className="btn-primary mt-7">Start prediction</Link></section></div>;
  return <div className="space-y-6 py-5 sm:py-8">
    <header className="rounded-[1.75rem] bg-[var(--navy)] px-6 py-8 text-white"><p className="flex items-center gap-2 text-[11px] font-black uppercase tracking-[.18em] text-[#9fccef]"><CheckCircle2 className="h-4 w-4"/>Prediction complete</p><h1 className="mt-2 font-display text-4xl font-semibold">DeepNEC results</h1><p className="mt-2 text-sm text-slate-300">Review predictions, annotations, motifs, and protein structures.</p></header>
    {error&&<div role="alert" className="rounded-xl border border-[#efb4c2] bg-[#fff4f7] p-4 text-sm font-semibold text-[var(--danger)]">{error}</div>}
    <section className="grid gap-3 sm:grid-cols-3"><div className="surface p-5"><p className="eyebrow">Outputs</p><p className="mt-2 font-display text-3xl">{files.length}</p></div><div className="surface p-5"><p className="eyebrow">Results ID</p><p className="mt-3 truncate font-mono text-xs font-bold">{data.jobId}</p></div><div className="surface p-5"><p className="eyebrow">Available</p><p className="mt-3 text-sm font-bold">{expiry?`Until ${expiry.toLocaleDateString()}`:'For 30 days'}</p></div></section>
    <nav className="grid gap-2 sm:grid-cols-2 lg:grid-cols-5">{files.map(file=>{const [label,title]=meta(file);return <button key={file} onClick={()=>setActive(file)} className={`rounded-xl border p-4 text-left ${active===file?'border-[var(--blue)] bg-[var(--blue)] text-white':'border-[var(--line)] bg-white'}`}><span className="text-[10px] font-black uppercase">{label}</span><span className="mt-1 block text-sm font-bold">{title}</span></button>})}</nav>
    <section className="surface overflow-hidden"><div className="flex flex-col gap-4 border-b p-5 sm:flex-row sm:items-center sm:justify-between"><div><p className="eyebrow">{meta(active)[0]}</p><h2 className="font-display text-2xl font-semibold">{meta(active)[1]}</h2></div><div className="flex flex-wrap gap-2"><button className="btn-secondary" onClick={()=>save(tsv(rows),active)}><Download className="h-4 w-4"/>TSV</button><button className="btn-secondary" onClick={()=>save(csv(rows),active.replace(/\.tsv$/,'.csv'),'text/csv')}><Download className="h-4 w-4"/>CSV</button><button className="btn-secondary" onClick={()=>save(JSON.stringify(rows,null,2),active.replace(/\.tsv$/,'.json'),'application/json')}><Download className="h-4 w-4"/>JSON</button></div></div><div className="overflow-x-auto"><table className="w-full min-w-[960px] text-left text-xs"><thead className="bg-[var(--ice)]"><tr>{rows[0]&&Object.keys(rows[0]).map(h=><th key={h} className="px-4 py-3 font-black uppercase">{h}</th>)}{bookmark&&<th className="px-4 py-3">STRUCTURE</th>}{active.includes('Phase_4')&&<th className="px-4 py-3">ANNOTATION</th>}</tr></thead><tbody className="divide-y">{rows.map((row,i)=>{const id=idOf(row),ec=String(row.EC_Number??row.Prediction??'');return <tr key={i}>{Object.values(row).map((v,j)=><td key={j} className="whitespace-nowrap px-4 py-3 font-mono">{String(v??'')}</td>)}{bookmark&&<td className="whitespace-nowrap px-4 py-3"><button className="mr-3 font-bold text-[var(--blue)]" disabled={!!busy} onClick={()=>void predict('secondary',id)}>{busy===`secondary:${id}`?'Running…':'Secondary'}</button><button className="font-bold text-[var(--pink)]" disabled={!!busy} onClick={()=>void predict('tertiary',id)}>{busy===`tertiary:${id}`?'Folding…':'3D'}</button></td>}{active.includes('Phase_4')&&<td className="whitespace-nowrap px-4 py-3">{links(ec).map(([label,url])=><a key={label} href={url} target="_blank" rel="noreferrer" className="mr-3 inline-flex items-center gap-1 font-bold text-[var(--blue)]">{label}<ExternalLink className="h-3 w-3"/></a>)}</td>}</tr>})}</tbody></table></div></section>
    {secondary&&<section className="surface p-6"><div className="flex justify-between"><div><p className="eyebrow">Secondary structure</p><h2 className="font-display text-2xl font-semibold">{secondary.sampleId}</h2></div><button className="btn-secondary" onClick={()=>save(`>${secondary.sampleId}\n${secondary.amino_acids}\n${secondary.prediction}\n`,`${secondary.sampleId}.secondary.fasta`,'text/plain')}><Download className="h-4 w-4"/>Download</button></div><div className="mt-5 overflow-x-auto rounded-xl bg-[var(--navy)] p-5 font-mono text-xs leading-6"><p className="text-white">{secondary.amino_acids}</p><p className="text-[#f08ba7]">{secondary.prediction}</p><p className="text-[#9fccef]">{secondary.confidence}</p></div><p className="mt-3 text-xs text-slate-500">H = helix, E = strand, C = coil.</p></section>}
    {tertiary&&<section className="surface p-6"><div className="flex justify-between"><div><p className="eyebrow">3D structure · {tertiary.method}</p><h2 className="font-display text-2xl font-semibold">{tertiary.sampleId}</h2></div><button className="btn-secondary" onClick={()=>save(tertiary.pdb,`${tertiary.sampleId}.pdb`,'chemical/x-pdb')}><Download className="h-4 w-4"/>Download PDB</button></div><div className="mt-5"><StructureViewer pdb={tertiary.pdb}/></div></section>}
  </div>;
}
